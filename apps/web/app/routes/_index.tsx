export default function IndexRoute() {
  return (
    <main className="page">
      <section className="hero">
        <p className="eyebrow">Data Wand</p>
        <h1>Remix Staging App</h1>
        <p>
          This app is the new full-stack product hub scaffold (auth, billing, dashboard) running independently from
          the current static marketing site.
        </p>
      </section>
    </main>
  );
}
